import fs from'node:fs';const js=fs.readFileSync('src/ed-premiere-clean-v1.js','utf8');const css=fs.readFileSync('src/xde-branching-v1.css','utf8');for(const term of ["6:'Once one company retains what works, the group can compound it.'","7:'So the next question is simple. Where should the proof begin?'","8:'The BDC was the proof. Your choice sets the next build.'"])if(!js.includes(term))throw new Error(`Concise Director narration missing: ${term}`);for(const branch of ['Finish the BDC operating system','Build one company operating layer','Run a Checkmate 1 comparison'])if(!js.includes(branch))throw new Error(`Continuation branch missing: ${branch}`);for(const term of ['function completeDocumentary()','PREMIERE COMPLETE','Continue with this proof',"label=completed?'Premiere Complete':'Continue to Second Appointment'",'onDecision&&!mandate'])if(!js.includes(term))throw new Error(`Completion contract missing: ${term}`);if(js.includes("start.addEventListener('click',begin,{once:true})"))throw new Error('One-use restart regression detected');for(const term of ['.branch-shell','contain:layout paint','.branch-options','.finale.completed'])if(!css.includes(term))throw new Error(`Branch compartment styling missing: ${term}`);console.log('PASS XDE branching and completion: locked concise Director narration, three continuation paths, required decision, working Complete action, and protected branch layout');
