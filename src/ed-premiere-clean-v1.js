const scenes=[...document.querySelectorAll('.scene')];const counter=document.querySelector('#counter');const progress=document.querySelector('#progress');const footer=document.querySelector('footer');const back=document.querySelector('#back');const next=document.querySelector('#next');const start=document.querySelector('#start');const restart=document.querySelector('#restart');const selectedPrinciple=document.querySelector('#selectedPrinciple');let index=0;let started=false;function render(){scenes.forEach((scene,i)=>scene.classList.toggle('active',i===index));counter.textContent=`${String(index+1).padStart(2,'0')} / ${String(scenes.length).padStart(2,'0')}`;progress.style.width=`${index/(scenes.length-1)*100}%`;back.disabled=index===0;next.disabled=index===scenes.length-1;next.textContent=scenes[index].dataset.label||'Continue';footer.classList.toggle('hidden',!started);scenes[index].scrollTop=0}function begin(){if(started)return;started=true;index=1;render()}function move(delta){if(!started)return;index=Math.max(1,Math.min(scenes.length-1,index+delta));render()}function reset(){started=false;index=0;document.querySelectorAll('.choice.selected').forEach(x=>x.classList.remove('selected'));selectedPrinciple.textContent='You taught the company.';render()}start.addEventListener('click',begin,{once:true});back.addEventListener('click',()=>move(-1));next.addEventListener('click',()=>move(1));restart.addEventListener('click',reset);document.addEventListener('click',event=>{const choice=event.target.closest('.choice');if(!choice)return;choice.parentElement.querySelectorAll('.choice').forEach(x=>x.classList.remove('selected'));choice.classList.add('selected');if(choice.dataset.principle)selectedPrinciple.textContent=choice.dataset.principle});document.addEventListener('keydown',event=>{if(!started)return;if(event.key==='ArrowLeft')move(-1);if(event.key==='ArrowRight')move(1)});render();